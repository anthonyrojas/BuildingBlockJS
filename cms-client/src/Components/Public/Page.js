import React, {Component} from 'react';
import {Grid} from '@material-ui/core';

class Page extends Component{
    render(){
        return(
            <Grid></Grid>
        );
    }
}

export default Page;